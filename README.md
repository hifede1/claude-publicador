# claude-publicador

> 🏗️ En construcción — el contrato VIGENTE está en `docs/FICHA.md`; el plan de obra, en el tracker (`docs/audits/`) y los issues #1–#5.

Pre-flight y publicación firmada: nada sale al mundo con un check rojo, nada se ejecuta sin firma humana.

## Instalación

```bash
claude plugin marketplace add hifede1/claude-audit-tracker
claude plugin install publicador@fede-tools
```

Comando: `/publicador:publicar` (v0.1.0 — esqueleto: declara el contrato, aún no verifica ni publica; ver el plan S02–S05).
