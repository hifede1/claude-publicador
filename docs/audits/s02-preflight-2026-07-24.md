# S02 — Pre-flight: corridas sembradas de los criterios 1 y 2

**Fecha:** 2026-07-24 · **Encargo:** issue #2 · **Motor:** gitleaks 8.30.1 (decisión firmada §4)
**Método:** corridas headless REALES (`claude --plugin-dir … -p "/publicador:publicar npm"`)
sobre repos sembrados. Transcripts completos en `s02-corridas/`.

## Corrida A — secreto en commit viejo (criterio 1) → ✅ BLOQUEA

Fixture `prueba-s02pub-secreto`: par de credenciales estilo AWS commiteado en `3d2171f` y
borrado de HEAD en el commit siguiente — la mina de n8n, calcada.

- Check 1 🔴: gitleaks lo encontró (**historia completa**, secreto REDACTADO en el informe,
  confirmado con los dos modos: `gitleaks git` y `detect --log-opts="--all"`).
- Check 2 🔴: el check casero también lo vio por camino independiente (`git log --all --name-only`).
- Veredicto: **⛔ NO SE PUBLICA** — y la remediación en el orden correcto: **rotar la
  credencial PRIMERO** (una clave que tocó un commit está quemada aunque se purgue), después
  `git filter-repo`, después re-scan hasta cero leaks.
- Bonus de rigor: cazó además el `repository` placeholder (duda = rojo) y el `.env` sin
  comodín en `.gitignore`.

## Corrida B — nombre tomado en npm (criterio 2) → ✅ DETECTA ANTES + SCOPED

Fixture `prueba-s02pub-nombre`: `package.json` con `name: "express"`.

- Check 5 🔴: `npm view express version` → 5.2.1 — **TOMADO, detectado ANTES de intentar
  publicar**, con la alternativa scoped propuesta (`@hifede/<nombre>` + cómo verificar que
  esté libre).
- Veredicto: **⛔ NO SE PUBLICA** con la lista exacta.
- Bonus: cazó que la LICENSE del fixture era una palabra suelta (no el texto MIT) — rojo legítimo
  que el sembrador no plantó a propósito.

## Hallazgo de máquina (real, no del fixture)

`npm whoami` → **E401: no hay sesión de npm en esta máquina**. El check 6 lo trató como manda
la regla: check que no pudo correr = ROJO, jamás verde. Para publicar de verdad hará falta
`npm login` (cuenta `hifede`) — dato para S04.

## Criterios del encargo (FICHA §6, criterios 1 y 2)
- «Secreto sembrado en commit viejo → detectado y BLOQUEADO» → ✅ (corrida A)
- «Nombre tomado → detectado ANTES, con alternativas scoped» → ✅ (corrida B)

Fase 3 sigue sin construir y ambas corridas lo declararon explícito — cero acciones de
escritura en las dos corridas (verificado: fixtures intactos, git status limpio).
